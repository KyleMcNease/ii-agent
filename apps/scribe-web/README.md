# Scribe Web Application

This directory hosts the Scribe web UI shell, real-time chat transport, and feature-specific adapters. Code that interacts with browsers or HTTP/WebSocket endpoints should live here rather than under `src/scribe_agents`.
